services:
  blog:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./blog.db:/app/blog.db
      - ./uploads:/app/uploads
      - ./static/uploads:/app/static/uploads
      - ./content:/app/content
    env_file:
      - .env
    restart: unless-stopped
